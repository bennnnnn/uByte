/** Maximum characters allowed in a single user chat message. */
export const MAX_USER_MESSAGE_LENGTH = 300;

/** Maximum number of user turns per chat session. */
export const MAX_SESSION_TURNS = 10;
