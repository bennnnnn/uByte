[
  {
    "title": "Basic for Loop",
    "instruction": "Go has one loop keyword: `for`. The classic form is `for init; condition; post { }`. Write a loop that prints the numbers 1 through 5, each on its own line.",
    "hint": "for i := 1; i <= 5; i++ { fmt.Println(i) }",
    "starter": "package main\n\nimport \"fmt\"\n\nfunc main() {\n\t// TODO: print numbers 1 to 5 using a for loop\n}",
    "expectedOutput": ["1", "2", "3", "4", "5"]
  },
  {
    "title": "While-Style Loop",
    "instruction": "Omitting init and post makes `for` act like a while loop. Count down from 3 to 1, printing each number, then print \"Liftoff!\" after the loop.",
    "hint": "for n > 0 { fmt.Println(n); n-- } then fmt.Println(\"Liftoff!\")",
    "starter": "package main\n\nimport \"fmt\"\n\nfunc main() {\n\tn := 3\n\t// TODO: loop while n > 0, print n, decrement n\n\t// TODO: after the loop, print \"Liftoff!\"\n\t_ = n\n}",
    "expectedOutput": ["3", "2", "1", "Liftoff!"]
  },
  {
    "title": "Break and Continue",
    "instruction": "`continue` skips the current loop turn and `break` stops the loop completely. Loop from 1 to 6. Skip even numbers, stop when the number is greater than 5, and print only the values that remain.",
    "hint": "Inside the loop: if `i%2 == 0`, continue. If `i > 5`, break. Otherwise print `i`.",
    "starter": "package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfor i := 1; i <= 6; i++ {\n\t\t// TODO: skip even numbers\n\t\t// TODO: stop the loop if i is greater than 5\n\t\t// TODO: print the remaining values\n\t}\n}",
    "expectedOutput": ["1", "3", "5"]
  },
  {
    "title": "Nested Loops",
    "instruction": "Use a nested loop to print a 2 by 3 grid of stars. Print `***` on the first line and `***` on the second line.",
    "hint": "Use one loop for rows and one loop for columns. Build each row as a string, then print it.",
    "starter": "package main\n\nimport \"fmt\"\n\nfunc main() {\n\t// TODO: use nested loops to print two lines of ***\n}",
    "expectedOutput": ["***", "***"]
  },
  {
    "title": "Sum with a Loop",
    "instruction": "Accumulate a running total. Sum all integers from 1 to 10 and print the result (should be 55).",
    "hint": "for i := 1; i <= 10; i++ { sum += i }",
    "starter": "package main\n\nimport \"fmt\"\n\nfunc main() {\n\tsum := 0\n\t// TODO: loop from 1 to 10 and add each to sum\n\tfmt.Println(sum)\n}",
    "expectedOutput": ["55"]
  }
]
