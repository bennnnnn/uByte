[
  {
    "title": "Define a Struct",
    "instruction": "Define a `Person` struct with `Name string` and `Age int`. Create a Person with name \"Alice\" and age 30, then print both fields on separate lines.",
    "hint": "type Person struct { Name string; Age int } — p := Person{Name: \"Alice\", Age: 30}",
    "starter": "package main\n\nimport \"fmt\"\n\n// TODO: define Person struct with Name and Age\n\nfunc main() {\n\t// TODO: create Person{Name: \"Alice\", Age: 30} and print Name then Age\n}",
    "expectedOutput": ["Alice", "30"]
  },
  {
    "title": "Read and Update Fields",
    "instruction": "Create a `Person` struct value for Alice with age 30. Increase the age to 31, then print the name and the updated age on separate lines.",
    "hint": "After creating the struct, update it with `p.Age = 31`.",
    "starter": "package main\n\nimport \"fmt\"\n\ntype Person struct {\n\tName string\n\tAge  int\n}\n\nfunc main() {\n\t// TODO: create Alice with age 30\n\t// TODO: change age to 31\n\t// TODO: print name and age\n}",
    "expectedOutput": ["Alice", "31"]
  },
  {
    "title": "Nested Structs",
    "instruction": "Define an `Address` struct with `City string`. Add an `Address` field to `Person`. Create a Person in London and print the city.",
    "hint": "type Address struct { City string }; type Person struct { Name string; Address Address }",
    "starter": "package main\n\nimport \"fmt\"\n\n// TODO: define Address with City, Person with Name and Address\n\nfunc main() {\n\tp := Person{\n\t\tName:    \"Alice\",\n\t\tAddress: Address{City: \"London\"},\n\t}\n\tfmt.Println(p.Address.City)\n}",
    "expectedOutput": ["London"]
  },
  {
    "title": "Anonymous Struct",
    "instruction": "Create an anonymous struct inline with `Name` and `Score` fields, set them to \"Bob\" and 95, and print both.",
    "hint": "entry := struct{ Name string; Score int }{Name: \"Bob\", Score: 95}",
    "starter": "package main\n\nimport \"fmt\"\n\nfunc main() {\n\t// TODO: anonymous struct with Name and Score, then print them\n}",
    "expectedOutput": ["Bob", "95"]
  },
  {
    "title": "Struct List with Slices",
    "instruction": "Create a slice of two `Person` values: Alice age 30 and Bob age 28. Print the name of the second person.",
    "hint": "Use `people := []Person{{Name: \"Alice\", Age: 30}, {Name: \"Bob\", Age: 28}}`.",
    "starter": "package main\n\nimport \"fmt\"\n\ntype Person struct {\n\tName string\n\tAge  int\n}\n\nfunc main() {\n\t// TODO: create a slice with Alice and Bob\n\t// TODO: print the second person's name\n}",
    "expectedOutput": ["Bob"]
  }
]
