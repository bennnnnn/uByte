[
  {
    "title": "What is an interface",
    "instruction": "An interface is like a job posting. It lists what you need to *do* — not who you need to *be*.\n\n\"Must be able to `MakeNoise() string`\" — any type that has that method qualifies. Dog? Yes. Cat? Yes. Toaster? If it has `MakeNoise()`, sure.\n\nRun this — both Dog and Cat satisfy the `Animal` interface because they both have `MakeNoise()`.",
    "starter": "package main\n\nimport \"fmt\"\n\ntype Animal interface {\n\tSpeak() string\n}\n\ntype Dog struct{ Name string }\ntype Cat struct{ Name string }\n\nfunc (d Dog) Speak() string { return \"Woof!\" }\nfunc (c Cat) Speak() string { return \"Meow!\" }\n\nfunc makeNoise(a Animal) {\n\tfmt.Println(a.Speak())\n}\n\nfunc main() {\n\tmakeNoise(Dog{Name: \"Rex\"})\n\tmakeNoise(Cat{Name: \"Whiskers\"})\n}",
    "expectedOutput": [
      "Woof!",
      "Meow!"
    ],
    "successMessage": "Dog says Woof, Cat says Meow — but `makeNoise()` didn't care which one it got. It just asked: \"Can you MakeNoise?\" That's the power of interfaces."
  },
  {
    "title": "Define and satisfy an interface",
    "instruction": "Your turn. Define a `Shape` interface that requires `Area() float64`.\n\nThen make both `Rectangle` and `Triangle` satisfy it by adding `Area()` methods to each.\n\nIf it has `Area()`, it's a Shape. Period.",
    "hint": "type Shape interface { Area() float64 }  func (r Rectangle) Area() float64 { return r.Width * r.Height }  func (t Triangle) Area() float64 { return 0.5 * t.Base * t.Height }",
    "starter": "package main\n\nimport \"fmt\"\n\n// TODO: define Shape interface with Area() float64\n\ntype Rectangle struct{ Width, Height float64 }\ntype Triangle struct{ Base, Height float64 }\n\n// TODO: add Area() to Rectangle\n// TODO: add Area() to Triangle\n\nfunc printArea(s Shape) {\n\tfmt.Printf(\"Area: %.2f\\n\", s.Area())\n}\n\nfunc main() {\n\tprintArea(Rectangle{Width: 5, Height: 3})\n\tprintArea(Triangle{Base: 6, Height: 4})\n}",
    "expectedOutput": [
      "Area: 15.00",
      "Area: 12.00"
    ],
    "codeChecks": [
      {
        "pattern": "type\\s+Shape\\s+interface\\s*\\{",
        "required": true,
        "message": "Define the Shape interface: type Shape interface { ... }"
      },
      {
        "pattern": "Area\\s*\\(\\s*\\)\\s*float64",
        "required": true,
        "message": "The interface must require Area() float64."
      },
      {
        "pattern": "func\\s*\\(\\s*\\w+\\s+Rectangle\\s*\\)\\s*Area",
        "required": true,
        "message": "Add Area() method to Rectangle."
      },
      {
        "pattern": "func\\s*\\(\\s*\\w+\\s+Triangle\\s*\\)\\s*Area",
        "required": true,
        "message": "Add Area() method to Triangle."
      },
      {
        "pattern": "TODO",
        "required": false,
        "message": "Replace the TODO comments with your code."
      }
    ],
    "successMessage": "Two different types, one interface. Go doesn't care about inheritance or class trees — just \"do you have the method?\""
  },
  {
    "title": "Interfaces with multiple methods",
    "instruction": "An interface can require any number of methods. A type must implement all of them to satisfy the interface.\n\nAdd `Perimeter() float64` to the `Shape` interface. Then add `Perimeter()` methods to both `Rectangle` and `Circle`. Update `describe` to print both.",
    "hint": "func (r Rectangle) Perimeter() float64 { return 2 * (r.Width + r.Height) }  func (c Circle) Perimeter() float64 { return 2 * 3.14159 * c.Radius }",
    "starter": "package main\n\nimport \"fmt\"\n\ntype Shape interface {\n\tArea() float64\n\t// TODO: add Perimeter() float64\n}\n\ntype Rectangle struct{ Width, Height float64 }\ntype Circle struct{ Radius float64 }\n\nfunc (r Rectangle) Area() float64 { return r.Width * r.Height }\nfunc (c Circle) Area() float64    { return 3.14159 * c.Radius * c.Radius }\n\n// TODO: add Perimeter() to Rectangle\n// TODO: add Perimeter() to Circle\n\nfunc describe(s Shape) {\n\tfmt.Printf(\"Area: %.2f, Perimeter: %.2f\\n\", s.Area(), s.Perimeter())\n}\n\nfunc main() {\n\tdescribe(Rectangle{Width: 4, Height: 3})\n\tdescribe(Circle{Radius: 5})\n}",
    "expectedOutput": [
      "Area: 12.00, Perimeter: 14.00",
      "Area: 78.54, Perimeter: 31.42"
    ],
    "codeChecks": [
      {
        "pattern": "Perimeter\\s*\\(\\s*\\)\\s*float64",
        "required": true,
        "message": "Add Perimeter() float64 to the Shape interface."
      },
      {
        "pattern": "func\\s*\\(\\s*\\w+\\s+Rectangle\\s*\\)\\s*Perimeter",
        "required": true,
        "message": "Add Perimeter() to Rectangle."
      },
      {
        "pattern": "func\\s*\\(\\s*\\w+\\s+Circle\\s*\\)\\s*Perimeter",
        "required": true,
        "message": "Add Perimeter() to Circle."
      },
      {
        "pattern": "TODO",
        "required": false,
        "message": "Replace the TODO comments with your code."
      }
    ],
    "successMessage": "Both methods, both types. If you add a method to the interface but forget to add it to a type, the compiler tells you immediately — no runtime surprises."
  },
  {
    "title": "A slice of interfaces",
    "instruction": "Because all three shapes satisfy `Shape`, you can store them in a `[]Shape` and treat them uniformly.\n\nAdd a `Circle{Radius: 3}` to the shapes slice, then loop through and call `describe` on each.",
    "hint": "shapes := []Shape{Rectangle{...}, Triangle{...}, Circle{Radius: 3}}",
    "starter": "package main\n\nimport \"fmt\"\n\ntype Shape interface {\n\tArea() float64\n\tName() string\n}\n\ntype Rectangle struct{ Width, Height float64 }\ntype Triangle struct{ Base, Height float64 }\ntype Circle struct{ Radius float64 }\n\nfunc (r Rectangle) Area() float64 { return r.Width * r.Height }\nfunc (t Triangle) Area() float64  { return 0.5 * t.Base * t.Height }\nfunc (c Circle) Area() float64    { return 3.14159 * c.Radius * c.Radius }\n\nfunc (r Rectangle) Name() string { return \"Rectangle\" }\nfunc (t Triangle) Name() string  { return \"Triangle\" }\nfunc (c Circle) Name() string    { return \"Circle\" }\n\nfunc describe(s Shape) {\n\tfmt.Printf(\"%s: area = %.2f\\n\", s.Name(), s.Area())\n}\n\nfunc main() {\n\tshapes := []Shape{\n\t\tRectangle{Width: 5, Height: 3},\n\t\tTriangle{Base: 6, Height: 4},\n\t\t// TODO: add Circle{Radius: 3}\n\t}\n\tfor _, s := range shapes {\n\t\tdescribe(s)\n\t}\n}",
    "expectedOutput": [
      "Rectangle: area = 15.00",
      "Triangle: area = 12.00",
      "Circle: area = 28.27"
    ],
    "codeChecks": [
      {
        "pattern": "Circle\\{Radius:\\s*3\\}",
        "required": true,
        "message": "Add Circle{Radius: 3} to the shapes slice."
      },
      {
        "pattern": "TODO",
        "required": false,
        "message": "Replace the TODO comment with your code."
      }
    ],
    "successMessage": "Three different types in one slice, one loop, one function. This is the power of interfaces — write the loop once, it works for every type that satisfies Shape."
  },
  {
    "title": "The Stringer interface",
    "instruction": "Remember `String()` from the Methods chapter? Surprise — that was an interface all along.\n\n`fmt.Stringer` requires one method: `String() string`. Any type with that method gets printed beautifully by `fmt.Println`.\n\nAdd `String()` to `Point` so it prints as `(3, 7)` instead of `{3 7}`.",
    "hint": "func (p Point) String() string { return fmt.Sprintf(\"(%d, %d)\", p.X, p.Y) }",
    "starter": "package main\n\nimport \"fmt\"\n\ntype Point struct {\n\tX, Y int\n}\n\n// TODO: add String() string method to Point\n\nfunc main() {\n\tp := Point{X: 3, Y: 7}\n\tfmt.Println(p)\n}",
    "expectedOutput": [
      "(3, 7)"
    ],
    "codeChecks": [
      {
        "pattern": "func\\s*\\(\\s*\\w+\\s+Point\\s*\\)\\s*String\\s*\\(\\s*\\)\\s*string",
        "required": true,
        "message": "Add String() string method to Point."
      },
      {
        "pattern": "Sprintf",
        "required": true,
        "message": "Use fmt.Sprintf inside String() to format the output."
      },
      {
        "pattern": "TODO",
        "required": false,
        "message": "Replace the TODO comment with your method."
      }
    ],
    "successMessage": "You satisfied an interface without even importing it. fmt just checked: \"Does this type have String()?\" Yes → use it. That's Go."
  },
  {
    "title": "Interface as function parameter",
    "instruction": "Interfaces shine when used as function parameters. The function does not care about the concrete type — only that it has the right methods.\n\n`io.Writer` is the most famous example. `fmt.Fprintf` takes any `io.Writer` — a file, a network connection, a buffer, anything.\n\nRun this. `logMessage` accepts any `io.Writer`. We pass `os.Stdout` — the terminal. The function works the same regardless of where the output goes.",
    "starter": "package main\n\nimport (\n\t\"fmt\"\n\t\"io\"\n\t\"os\"\n)\n\nfunc logMessage(w io.Writer, msg string) {\n\tfmt.Fprintf(w, \"[LOG] %s\\n\", msg)\n}\n\nfunc main() {\n\tlogMessage(os.Stdout, \"Server started\")\n\tlogMessage(os.Stdout, \"Request received\")\n\tlogMessage(os.Stdout, \"Response sent\")\n}",
    "expectedOutput": [
      "[LOG] Server started",
      "[LOG] Request received",
      "[LOG] Response sent"
    ],
    "successMessage": "logMessage does not know it is writing to a terminal. It just knows it has a Writer. Swap os.Stdout for a file or a network socket and the function does not change at all."
  },
  {
    "title": "Type assertion",
    "instruction": "Sometimes you have an interface value and you need to access the concrete type underneath.\n\n`concrete, ok := interfaceVal.(ConcreteType)`\n\nIf the underlying type is `ConcreteType`, `ok` is `true` and `concrete` has the concrete value. If not, `ok` is `false` and `concrete` is the zero value.\n\nRun this. Then add a type assertion to check if `s` is a `Circle` and print its radius if it is.",
    "hint": "if c, ok := s.(Circle); ok { fmt.Println(\"Circle radius:\", c.Radius) }",
    "starter": "package main\n\nimport \"fmt\"\n\ntype Shape interface{ Area() float64 }\n\ntype Rectangle struct{ Width, Height float64 }\ntype Circle struct{ Radius float64 }\n\nfunc (r Rectangle) Area() float64 { return r.Width * r.Height }\nfunc (c Circle) Area() float64    { return 3.14159 * c.Radius * c.Radius }\n\nfunc inspect(s Shape) {\n\tif r, ok := s.(Rectangle); ok {\n\t\tfmt.Printf(\"Rectangle: %.0f x %.0f\\n\", r.Width, r.Height)\n\t}\n\t// TODO: add type assertion for Circle and print its Radius\n}\n\nfunc main() {\n\tinspect(Rectangle{Width: 4, Height: 3})\n\tinspect(Circle{Radius: 5})\n}",
    "expectedOutput": [
      "Rectangle: 4 x 3",
      "Circle radius: 5"
    ],
    "codeChecks": [
      {
        "pattern": "s\\.\\(Circle\\)",
        "required": true,
        "message": "Add a type assertion: if c, ok := s.(Circle); ok { ... }"
      },
      {
        "pattern": "c\\.Radius|Radius:",
        "required": true,
        "message": "Print the Circle's Radius."
      },
      {
        "pattern": "TODO",
        "required": false,
        "message": "Replace the TODO comment with your assertion."
      }
    ],
    "successMessage": "The two-value assertion (value, ok) is the safe version. Never use the one-value version s.(Circle) without ok — it panics if the type is wrong."
  },
  {
    "title": "Type switch",
    "instruction": "When you need to handle multiple concrete types from an interface, a type switch is cleaner than chaining type assertions.\n\n```\nswitch v := i.(type) {\ncase int:\n    fmt.Println(\"int:\", v)\ncase string:\n    fmt.Println(\"string:\", v)\ndefault:\n    fmt.Println(\"unknown\")\n}\n```\n\nAdd a `case Triangle` to the type switch that prints the base and height.",
    "hint": "case Triangle: fmt.Printf(\"Triangle: base=%.0f height=%.0f\\n\", v.Base, v.Height)",
    "starter": "package main\n\nimport \"fmt\"\n\ntype Shape interface{ Area() float64 }\n\ntype Rectangle struct{ Width, Height float64 }\ntype Triangle struct{ Base, Height float64 }\ntype Circle struct{ Radius float64 }\n\nfunc (r Rectangle) Area() float64 { return r.Width * r.Height }\nfunc (t Triangle) Area() float64  { return 0.5 * t.Base * t.Height }\nfunc (c Circle) Area() float64    { return 3.14159 * c.Radius * c.Radius }\n\nfunc whatShape(s Shape) {\n\tswitch v := s.(type) {\n\tcase Rectangle:\n\t\tfmt.Printf(\"Rectangle: %.0f x %.0f\\n\", v.Width, v.Height)\n\t// TODO: add case Triangle\n\tcase Circle:\n\t\tfmt.Printf(\"Circle: radius=%.0f\\n\", v.Radius)\n\tdefault:\n\t\tfmt.Println(\"Unknown shape\")\n\t}\n}\n\nfunc main() {\n\twhatShape(Rectangle{Width: 4, Height: 3})\n\twhatShape(Triangle{Base: 6, Height: 4})\n\twhatShape(Circle{Radius: 5})\n}",
    "expectedOutput": [
      "Rectangle: 4 x 3",
      "Triangle: base=6 height=4",
      "Circle: radius=5"
    ],
    "codeChecks": [
      {
        "pattern": "case\\s+Triangle:",
        "required": true,
        "message": "Add case Triangle: to the type switch."
      },
      {
        "pattern": "v\\.Base|v\\.Height",
        "required": true,
        "message": "Print the Triangle's Base and Height using v."
      },
      {
        "pattern": "TODO",
        "required": false,
        "message": "Replace the TODO comment with your case."
      }
    ],
    "successMessage": "Type switch: one construct, multiple types, zero repetition. Much cleaner than three consecutive if-else type assertions."
  },
  {
    "title": "The empty interface",
    "instruction": "`interface{}` (or `any` in newer Go) has zero methods — which means *every* type satisfies it.\n\nThink of it as a box that accepts anything: ints, strings, bools, slices, your custom types.\n\nUseful for generic containers, but use sparingly — you lose type safety.",
    "hint": "printAnything([]int{1, 2, 3})",
    "starter": "package main\n\nimport \"fmt\"\n\nfunc printAnything(v any) {\n\tfmt.Printf(\"Type: %T | Value: %v\\n\", v, v)\n}\n\nfunc main() {\n\tprintAnything(42)\n\tprintAnything(\"hello\")\n\tprintAnything(true)\n\tprintAnything(3.14)\n\t// TODO: call printAnything with []int{1, 2, 3}\n}",
    "expectedOutput": [
      "Type: int | Value: 42",
      "Type: string | Value: hello",
      "Type: bool | Value: true",
      "Type: float64 | Value: 3.14",
      "Type: []int | Value: [1 2 3]"
    ],
    "codeChecks": [
      {
        "pattern": "printAnything\\(\\[\\]int\\{1,\\s*2,\\s*3\\}\\)",
        "required": true,
        "message": "Call printAnything([]int{1, 2, 3})."
      },
      {
        "pattern": "TODO",
        "required": false,
        "message": "Replace the TODO comment with your call."
      }
    ],
    "successMessage": "The empty interface accepts everything. Powerful, but it's like a drawer labeled \"stuff\" — you'll need type assertions to figure out what's actually inside."
  },
  {
    "title": "Composing interfaces",
    "instruction": "Interfaces can embed other interfaces. The composed interface requires all methods from all embedded interfaces.\n\n```\ntype ReadWriter interface {\n    Reader  // embeds Reader (requires Read)\n    Writer  // embeds Writer (requires Write)\n}\n```\n\nThis is how Go's standard library is built. `io.ReadWriter` is just `io.Reader` + `io.Writer`.\n\nRun this. `Saver` embeds both `Storer` and `Retriever`. `MemoryStore` must implement all three methods.",
    "starter": "package main\n\nimport \"fmt\"\n\ntype Storer interface {\n\tStore(key, value string)\n}\n\ntype Retriever interface {\n\tRetrieve(key string) string\n}\n\ntype Saver interface {\n\tStorer\n\tRetriever\n}\n\ntype MemoryStore struct {\n\tdata map[string]string\n}\n\nfunc (m *MemoryStore) Store(key, value string) {\n\tif m.data == nil {\n\t\tm.data = make(map[string]string)\n\t}\n\tm.data[key] = value\n}\n\nfunc (m *MemoryStore) Retrieve(key string) string {\n\treturn m.data[key]\n}\n\nfunc saveAndGet(s Saver, key, value string) {\n\ts.Store(key, value)\n\tfmt.Println(s.Retrieve(key))\n}\n\nfunc main() {\n\tstore := &MemoryStore{}\n\tsaveAndGet(store, \"name\", \"Alex\")\n\tsaveAndGet(store, \"city\", \"London\")\n}",
    "expectedOutput": [
      "Alex",
      "London"
    ],
    "successMessage": "MemoryStore satisfies Saver by implementing both Store and Retrieve. Interface composition lets you build precise, minimal contracts — the Go standard library uses this pattern throughout."
  },
  {
    "title": "Implicit satisfaction — no implements keyword",
    "instruction": "This is the most important thing about Go interfaces.\n\nIn Java, you write `implements Speaker`. In Go, you just... have the method. No keyword. No declaration. If your type has `Speak()`, it *is* a Speaker.\n\nThis means you can satisfy interfaces from packages you've never imported. Run this — Dog never mentions Speaker, but it works.",
    "starter": "package main\n\nimport \"fmt\"\n\ntype LoudDog struct{ Name string }\n\nfunc (d LoudDog) String() string {\n\treturn fmt.Sprintf(\"WOOF! I AM %s AND I AM VERY LOUD!\", d.Name)\n}\n\nfunc main() {\n\tdog := LoudDog{Name: \"Rex\"}\n\tfmt.Println(dog)\n\tfmt.Printf(\"The dog says: %v\\n\", dog)\n}",
    "expectedOutput": [
      "WOOF! I AM Rex AND I AM VERY LOUD!",
      "The dog says: WOOF! I AM Rex AND I AM VERY LOUD!"
    ],
    "successMessage": "No `implements`, no `extends`, no ceremony. Just methods. This is why Go interfaces are so flexible — any type can satisfy any interface, even one written years later by someone else."
  },
  {
    "title": "Fix the broken interface",
    "instruction": "Bug hunt time! The code tries to use `Robot` as a `Speaker`, but it won't compile.\n\nRead the error, figure out what's missing, and fix it.",
    "hint": "Robot needs a Speak() string method. Add: func (r Robot) Speak() string { return \"Beep boop, I am \" + r.Model }",
    "starter": "package main\n\nimport \"fmt\"\n\ntype Speaker interface {\n\tSpeak() string\n}\n\ntype Human struct{ Name string }\ntype Robot struct{ Model string }\n\nfunc (h Human) Speak() string { return \"Hello, I am \" + h.Name }\n\n// BUG: Robot does not implement Speaker — add the missing method\n\nfunc introduce(s Speaker) {\n\tfmt.Println(s.Speak())\n}\n\nfunc main() {\n\tintroduce(Human{Name: \"Alex\"})\n\tintroduce(Robot{Model: \"R2D2\"})\n}",
    "expectedOutput": [
      "Hello, I am Alex",
      "Beep boop, I am R2D2"
    ],
    "codeChecks": [
      {
        "pattern": "func\\s*\\(\\s*\\w+\\s+Robot\\s*\\)\\s*Speak\\s*\\(\\s*\\)\\s*string",
        "required": true,
        "message": "Add Speak() string method to Robot."
      },
      {
        "pattern": "Beep boop",
        "required": true,
        "message": "Robot's Speak() should return a string starting with \"Beep boop\"."
      }
    ],
    "successMessage": "Robot was missing the `Speak()` method. No method = doesn't satisfy the interface. The compiler catches this for you — that's the safety net."
  },
  {
    "title": "Build a notification system",
    "instruction": "Build a notification system from scratch.\n\n1. Define a `Notifier` interface: `Notify(message string)`\n2. Create three types: `EmailNotifier`, `SMSNotifier`, `PushNotifier`\n3. Each prints its own format (see expected output)\n4. Write a `sendAll()` function that takes `[]Notifier` and a message\n\nThis is how real systems work — Slack, email, SMS all behind one interface.",
    "hint": "func (e EmailNotifier) Send(msg string) string { return fmt.Sprintf(\"Email sent to %s: %s\", e.To, msg) }",
    "starter": "package main\n\nimport \"fmt\"\n\n// Build your notification system here\n\nfunc main() {\n}",
    "expectedOutput": [
      "Email sent to alex@email.com: Hello",
      "SMS sent to +1234567890: Hello",
      "Push sent to device-abc123: Hello"
    ],
    "codeChecks": [
      {
        "pattern": "type\\s+Notifier\\s+interface",
        "required": true,
        "message": "Define a Notifier interface."
      },
      {
        "pattern": "Send\\s*\\(\\s*\\w+\\s+string\\s*\\)\\s*string",
        "required": true,
        "message": "The Notifier interface must have Send(message string) string."
      },
      {
        "pattern": "type\\s+EmailNotifier\\s+struct",
        "required": true,
        "message": "Define an EmailNotifier struct."
      },
      {
        "pattern": "type\\s+SMSNotifier\\s+struct",
        "required": true,
        "message": "Define an SMSNotifier struct."
      },
      {
        "pattern": "type\\s+PushNotifier\\s+struct",
        "required": true,
        "message": "Define a PushNotifier struct."
      },
      {
        "pattern": "\\[\\]Notifier",
        "required": true,
        "message": "Create a []Notifier slice."
      },
      {
        "pattern": "range\\s+\\w+",
        "required": true,
        "message": "Loop through the notifiers with range."
      },
      {
        "pattern": "\\.Send\\(\"Hello\"\\)",
        "required": true,
        "message": "Call .Send(\"Hello\") on each notifier."
      }
    ],
    "successMessage": "Three notification channels, one interface, one function. Add a fourth channel later? Just implement Notify(). Nothing else changes. That's the beauty of interfaces."
  }
]