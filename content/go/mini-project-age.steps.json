[
  {
    "title": "Set an age",
    "instruction": "Create a variable `age` with `:=` and pick any whole number (for example `28`). Print **one** line that includes that number and the word `years` so it reads like a real sentence.",
    "hint": "age := 28 — then fmt.Println(\"I am\", age, \"years old\") or similar.",
    "starter": "package main\n\nimport \"fmt\"\n\nfunc main() {\n\t// age will hold how many years old someone is (you choose the number).\n\t// TODO: use := to create age with a number you like\n\t// TODO: fmt.Println a single sentence that includes age and \"years\"\n}",
    "expectedOutput": [],
    "codeChecks": [
      { "pattern": "age\\s*:=\\s*\\d+", "required": true, "message": "Create age with := and a number, e.g. age := 28" },
      { "pattern": "Println\\(", "required": true, "message": "Print a line with fmt.Println" },
      { "pattern": "years", "flags": "i", "required": true, "message": "Include the word \"years\" in your output string(s)." },
      { "pattern": "TODO", "required": false, "message": "Replace the TODO lines with real code." }
    ],
    "successMessage": "You stored a number in `age` and showed it in a sentence — same idea as every profile or score in an app.",
    "carryForward": true
  },
  {
    "title": "Jump ten years ahead",
    "instruction": "Keep your `age` value as-is. Add a **new** variable `futureAge` with `:=` set to `age + 10`. Print `age` on one line and `futureAge` on the next (or two clear lines so both numbers appear).",
    "hint": "futureAge := age + 10 — then two Println calls.",
    "starter": "package main\n\nimport \"fmt\"\n\nfunc main() {\n\tage := 28 // your age from the last step (change the number if you like)\n\tfmt.Println(\"Current age:\", age)\n\n\t// futureAge will be age plus 10 — a second variable so both values stay visible.\n\t// TODO: futureAge := age + 10\n\t// TODO: print futureAge on its own line\n}",
    "expectedOutput": [],
    "codeChecks": [
      { "pattern": "futureAge\\s*:=\\s*age\\s*\\+\\s*10", "required": true, "message": "Add futureAge := age + 10" },
      { "pattern": "Println\\(.*age", "required": true, "message": "Print the current age" },
      { "pattern": "Println\\(.*futureAge", "required": true, "message": "Print futureAge on another line" },
      { "pattern": "TODO", "required": false, "message": "Replace the TODO lines with real code." }
    ],
    "successMessage": "Same program, more story: you reused `age` and expressed a new value without throwing away the old one. That is how small programs grow."
  }
]
