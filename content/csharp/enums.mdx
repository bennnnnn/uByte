---
title: "Enums"
description: "Define enumerated types, use Flags enums for bit fields, and parse enums from strings."
order: 14
difficulty: "beginner"
estimatedMinutes: 15
concepts: ["enum", "Flags", "HasFlag", "Enum.Parse", "GetValues", "underlying type"]
---

## Named constants

Enums give meaningful names to sets of related integer constants.

C# enums are strongly typed — you cannot accidentally use an invalid value or mix two different enum types.
