---
title: "LINQ Basics"
description: "Query collections with LINQ: Where, Select, OrderBy, GroupBy, First, Aggregate."
order: 17
difficulty: "intermediate"
estimatedMinutes: 20
concepts: ["LINQ", "Where", "Select", "OrderBy", "GroupBy", "First", "Any", "All", "Aggregate", "method syntax"]
---

## Language Integrated Query

LINQ adds SQL-like query capabilities directly to C# collections. It works on any `IEnumerable<T>` — arrays, lists, databases, XML.

LINQ operations are lazy — they run when you iterate or call ToList/ToArray.
