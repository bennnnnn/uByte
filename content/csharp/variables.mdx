---
title: "Variables"
description: "Declare variables with var and explicit types, use const and readonly, and understand C# type inference."
order: 2
difficulty: "beginner"
estimatedMinutes: 15
concepts: ["var", "int", "string", "bool", "double", "const", "readonly", "type inference"]
---

## Storing data

Variables are named containers for data. C# is statically typed — every variable has a type known at compile time.

You can be explicit (`int x = 5;`) or use `var` to let the compiler infer the type (`var x = 5;`).
