---
title: "HTTP Basics"
description: "Make HTTP requests with HttpClient, parse JSON responses, and handle HTTP errors."
order: 25
difficulty: "intermediate"
estimatedMinutes: 20
concepts: ["HttpClient", "GetAsync", "PostAsync", "GetStringAsync", "JsonSerializer", "HttpResponseMessage", "StatusCode"]
---

## HTTP and the web

`HttpClient` is the modern .NET way to make HTTP requests. Combine it with `async/await` for non-blocking network calls.

`System.Text.Json` is the built-in JSON serialiser — fast, lightweight, and part of the standard library.
