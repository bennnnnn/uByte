---
title: "Arrays"
description: "Single and multi-dimensional arrays, iterating, sorting, and Array class methods."
order: 8
difficulty: "beginner"
estimatedMinutes: 15
concepts: ["array", "[]", "Length", "foreach", "Array.Sort", "Array.Reverse", "2D arrays"]
---

## Fixed-size collections

Arrays store a fixed number of elements of the same type. The size is set at creation and cannot change.

For dynamic collections, use `List<T>` (next chapter).
