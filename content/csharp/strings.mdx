---
title: "Strings"
description: "String creation, interpolation, formatting, and common string methods in C#."
order: 4
difficulty: "beginner"
estimatedMinutes: 18
concepts: ["string", "String", "interpolation", "string interpolation", "methods", "StringBuilder", "Compare", "Split"]
---

## Working with text

`string` in C# is a reference type, but it behaves like a value type — it is immutable. Every operation that seems to modify a string actually creates a new one.

For heavy string building, use `StringBuilder` to avoid creating many intermediate strings.
