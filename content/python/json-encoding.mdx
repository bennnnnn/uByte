---
title: "JSON in Python"
description: "Speak the language of APIs. json.loads, json.dumps, nested data, and custom serializers."
order: 24
difficulty: "intermediate"
estimatedMinutes: 16
concepts: ["json.loads", "json.dumps", "json.dump", "json.load", "indent", "custom serializer", "JSONDecodeError"]
---

## JSON is everywhere

REST APIs return JSON. Config files are JSON. You will read and write JSON every single day as a Python developer.

Python's `json` module makes it trivial. Ten steps.
