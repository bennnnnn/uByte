---
title: "Async & Concurrency"
description: "Do multiple things at once without threads. async/await and asyncio make concurrent code readable."
order: 22
difficulty: "advanced"
estimatedMinutes: 22
concepts: ["async", "await", "asyncio", "coroutine", "event loop", "gather", "asyncio.sleep", "threading basics"]
---

## Don't wait around

Fetching a URL takes 200ms. Querying a database takes 100ms. Waiting for user input could take forever.

`async/await` lets your program do other work while waiting — without threads, without complexity. Twelve steps.
