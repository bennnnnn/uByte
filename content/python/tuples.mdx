---
title: "Tuples"
description: "Like a list, but locked. Tuples are immutable — perfect for data that should never change."
order: 9
difficulty: "beginner"
estimatedMinutes: 16
concepts: ["tuple", "immutability", "tuple unpacking", "named tuples", "single-element tuple", "when to use tuples vs lists"]
---

## Some data should not change

Coordinates, RGB colours, database rows, days of the week — these values are fixed. They should never be modified.

A tuple is an immutable list. Once you create it, the values are locked. Python uses this for safety and performance. Ten steps.
