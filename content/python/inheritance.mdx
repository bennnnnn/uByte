---
title: "Inheritance"
description: "Build on top of existing classes. Extend, override, and reuse without copying code."
order: 14
difficulty: "intermediate"
estimatedMinutes: 20
concepts: ["inheritance", "super()", "method override", "isinstance", "issubclass", "multiple inheritance", "MRO"]
---

## Build on what already exists

You have a `User` class. Now you need `AdminUser` — just like `User` but with extra powers.

Inheritance lets a new class inherit everything from an existing one, then add or change only what is different. Ten steps.
