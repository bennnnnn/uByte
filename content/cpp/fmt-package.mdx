---
title: "Strings"
description: "std::string — methods, concatenation, getline, string operations, string_view, and C-strings vs std::string."
order: 4
difficulty: "beginner"
estimatedMinutes: 18
concepts: ["std::string", "string methods", "length", "substr", "find", "replace", "getline", "stringstream", "string_view", "c-string", "to_string", "stoi"]
---

## Text in C++

C++ has two string types: the ancient C-string (`char array`) and the modern `std::string`. Always use `std::string` — it manages memory automatically and has a rich API.

`string_view` (C++17) is the efficient read-only alternative — it avoids copies when you just need to look at string data.
