---
title: "HTTP & fetch"
description: "Make HTTP requests with fetch, handle GET/POST/PUT/DELETE, work with headers, authentication, and graceful error handling."
order: 21
difficulty: "intermediate"
estimatedMinutes: 22
concepts: ["fetch", "Response", "json()", "GET", "POST", "headers", "Authorization", "AbortController", "error handling", "URLSearchParams"]
---

## Connect to the world

Every modern app talks to APIs. `fetch` is the browser and Node.js native HTTP client — clean, Promise-based, and powerful.

After this chapter, you'll be able to talk to any REST API — GitHub, Stripe, Twitter, your own backend.
