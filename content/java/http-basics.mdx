---
title: "HTTP & JSON"
description: "Make HTTP requests with Java's built-in HttpClient, parse JSON responses, build REST clients, and handle async HTTP."
order: 21
difficulty: "advanced"
estimatedMinutes: 22
concepts: ["HttpClient", "HttpRequest", "HttpResponse", "GET", "POST", "headers", "JSON parsing", "ObjectMapper concept", "async requests", "CompletableFuture"]
---

## Connect to the world

Java 11+ ships with `java.net.http.HttpClient` — a modern, async-capable HTTP client with no external dependencies.

Combined with JSON parsing, you can build REST clients, call APIs, and integrate with any web service — all in standard Java.
