---
title: "Pattern Matching"
description: "master match expressions, destructuring, guards, and the @ binding operator."
order: 12
difficulty: "intermediate"
estimatedMinutes: 20
concepts: ["match", "patterns", "destructuring", "guard", "@", "while let", "matches!"]
---

## Pattern matching

Rust's `match` is one of its most powerful features. It is exhaustive, expressive, and safe.

Patterns appear in many places: `let`, `if let`, `while let`, `for`, function parameters, and `match`.
