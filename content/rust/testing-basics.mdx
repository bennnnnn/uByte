---
title: "Testing"
description: "Write unit tests with #[test], use assert macros, and organise tests in Rust."
order: 24
difficulty: "intermediate"
estimatedMinutes: 18
concepts: ["#[test]", "assert_eq!", "assert!", "assert_ne!", "#[should_panic]", "test module", "Result in tests"]
---

## Built-in testing

Rust has first-class testing support built into the language. No external frameworks needed for unit tests.

Run tests with `cargo test`.
