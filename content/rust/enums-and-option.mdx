---
title: "Enums and Option"
description: "Define enums with data, use Option to represent nullable values, and avoid null pointer bugs."
order: 11
difficulty: "intermediate"
estimatedMinutes: 20
concepts: ["enum", "Option", "Some", "None", "if let", "unwrap", "match with data"]
---

## Enums carry data

Rust enums are much more powerful than in most languages — each variant can carry different data.

`Option<T>` is the built-in enum that replaces null. No more null pointer exceptions.
