---
title: "Control Flow"
description: "if/else expressions, loop, while, for — directing program execution in Rust."
order: 5
difficulty: "beginner"
estimatedMinutes: 18
concepts: ["if", "else if", "else", "loop", "while", "for", "break", "continue", "range"]
---

## Directing execution

Control flow decides which code runs and how many times.

In Rust, `if` is an *expression* — it returns a value. This is different from most languages and is one of Rust's most elegant features.
