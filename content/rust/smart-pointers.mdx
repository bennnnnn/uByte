---
title: "Smart Pointers"
description: "Box<T> for heap allocation, Rc<T> for shared ownership, and RefCell<T> for interior mutability."
order: 22
difficulty: "advanced"
estimatedMinutes: 22
concepts: ["Box", "Rc", "RefCell", "Deref", "Drop", "heap", "reference counting"]
---

## Pointers with power

Smart pointers are data structures that act like pointers but provide additional capabilities.

The most common: `Box<T>` (unique heap allocation), `Rc<T>` (shared ownership), `RefCell<T>` (runtime borrow checking).
